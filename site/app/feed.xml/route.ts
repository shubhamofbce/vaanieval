import { posts, siteConfig } from '@/lib/site'
export function GET(){const items=posts.map(p=>`<item><title><![CDATA[${p.title}]]></title><link>${siteConfig.url}/blog/${p.slug}</link><guid>${siteConfig.url}/blog/${p.slug}</guid><pubDate>${new Date(p.publishedAt).toUTCString()}</pubDate><description><![CDATA[${p.description}]]></description></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>VaaniEval field notes</title><link>${siteConfig.url}/blog</link><description>Practical voice AI evaluation and QA guidance.</description>${items}</channel></rss>`;return new Response(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
