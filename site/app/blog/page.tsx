import type { Metadata } from 'next'; import Link from 'next/link'; import { PageHero } from '@/components/PageHero'; import { posts } from '@/lib/site'
export const metadata:Metadata={title:'Voice AI evaluation blog',description:'Practical guides to production voice-agent QA, evaluation metrics, self-hosting, and conversation review.',alternates:{canonical:'/blog'}}
export default function Page(){return <><PageHero eyebrow="VaaniEval field notes" title="Practical voice AI evaluation guidance." description="Detailed, implementation-oriented writing for teams reviewing and improving production voice agents."/><section className="section shell"><div className="post-grid">{posts.map(p=><Link className="post-card" href={`/blog/${p.slug}`} key={p.slug}><span>{p.category}</span><h2>{p.title}</h2><p>{p.description}</p><small>{p.publishedAt} · {p.readingTime}</small></Link>)}</div></section></>}
