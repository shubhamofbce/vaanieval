import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { posts, siteConfig } from '@/lib/site'
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)return{};return{title:p.title,description:p.description,alternates:{canonical:`/blog/${p.slug}`},openGraph:{type:'article',title:p.title,description:p.description,publishedTime:p.publishedAt,url:`/blog/${p.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();const ld={'@context':'https://schema.org','@type':'Article',headline:p.title,description:p.description,datePublished:p.publishedAt,author:{'@type':'Organization',name:'VaaniEval'},publisher:{'@type':'Organization',name:'VaaniEval'},mainEntityOfPage:`${siteConfig.url}/blog/${p.slug}`};return <article><header className="page-hero shell"><p className="eyebrow">{p.category}</p><h1>{p.title}</h1><p className="lede">{p.description}</p><div className="article-meta"><time>{p.publishedAt}</time><span>·</span><span>{p.readingTime}</span></div></header><div className="content-shell article-body">{p.content.map((x,i)=><p key={i}>{x}</p>)}</div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/></article>}
