import type { MetadataRoute } from 'next'; import { posts, siteConfig } from '@/lib/site'
export default function sitemap():MetadataRoute.Sitemap{const paths=['','/voice-ai-evaluation','/voice-agent-qa','/integrations/elevenlabs','/integrations/vapi','/open-source','/design-partners','/resources/voice-agent-qa-checklist','/resources/evaluation-metrics','/blog','/privacy'];return[...paths.map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path===''?'weekly' as const:'monthly' as const,priority:path===''?1:.7})),...posts.map(p=>({url:`${siteConfig.url}/blog/${p.slug}`,lastModified:new Date(p.publishedAt),changeFrequency:'monthly' as const,priority:.7}))]}
